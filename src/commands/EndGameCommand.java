package commands;

public class EndGameCommand implements Command {
    @Override
    public void execute() {
        //TODO end game command
        // Display stuff in the UI
        // (this will be called by the strategy)
    }
}
